# 待办事项

## 当前优先级：V5 GRPO 稳定跑完

**状态**：进行中

1. 明确下一次使用哪个 GRPO launcher：
   - 已完成：旧 GRPO launcher 已删除。
   - 当前唯一 GRPO 入口：`RL/scripts/train_grpo_ovo_8gpu.sh`
   - PPO 入口保留：`RL/scripts/train_ppo.sh`
2. 确认 checkpoint 策略：
   - 当前 GRPO 入口是 `save_freq=30` 和 `resume_mode=auto`，但需要完整验证。
3. 明确记录模型起点是 answered-full SFT vLLM 兼容模型。
4. 重跑 `<120s` OVO RL 子集，确认：
   - 中途能保存 checkpoint
   - 失败后能恢复
   - run 目录有明确 `exit_code.txt` 或等价结束状态
5. 训练侧性能优化：
   - 优先看 `old_log_prob` 和 `update_actor`
   - 重点排查 remove padding、FSDP2 offload、长序列 token 量和 micro batch 设置
6. 首个完整 checkpoint 出来后：
   - 跑 OVO 小规模回评
   - 对比 base instruct / SFT checkpoint / RL checkpoint
   - 记录 reward 曲线和真实 eval 是否一致

## 需要确认

- 当前 RL 默认从 `models/qwen3vl8b_streamweave_sft_answered_full_vllm` 开始。
- 如果改用 `/mmu_mllm_hdd/Models/Qwen3-VL-8B-Instruct`，需要单独开 run name，避免和 SFT-init 结果混淆。
- 旧 8GPU run 和旧 fused/chunked run 已清理，不再恢复；新 run 需要确认 reward v2 配置和日志字段生效。

## 暂缓事项

- 语义相似度 evaluator：等 GRPO 能稳定完成并保存 checkpoint 后再接。
- 更复杂的 memory reward：先用 note 数、bridge token 和格式/成功分做可控版本。
- 扩大 RL 数据规模：先把 `<120s` 子集跑完整。
- 继续堆 SFT 数据：第一次 SFT 1/8 回评退化，当前不应优先扩大同口径 SFT。

## 已完成但需要保留的结论

- OVO 单 query RL 数据已生成：
  - `ovo_rl.json`：约 `600` 条，三类各约 `200`
  - `ovo_rl_lt120s.json`：`293` 条
- V4 SFT 合成链路已打通，但第一次 SFT 回评退化。
- V3 + Gemini 在 OVO full 上拿到 `65.81`，overall 略超 AURA，但 Forward/CRR 仍弱。
