# 命令和工具使用

本文只放当前仍建议使用的命令。旧实验脚本和历史 run 对应关系见 `04-SFT训练.md`、`05-RL训练.md`；跑分 output 见 `06-实验跑分.md`。

## 基本路径

进入仓库：

```bash
cd /mmu_mllm_hdd/zhouhanshu/test/exp3/streamweave_v5
```

常用 Python：

```bash
/mmu_mllm_hdd/zhouhanshu/conda/envs/simple/bin/python
```

RL Python：

```bash
/mmu_mllm_hdd/zhouhanshu/conda/envs/verl_0425/bin/python
```

LLaMAFactory Python：

```bash
/mmu_mllm_hdd/zhouhanshu/conda/envs/llama_0425/bin/python
```

Gemini credentials：

```bash
export GOOGLE_APPLICATION_CREDENTIALS=/mmu_ssd3/group_lisize/hetu/xujia10/joint_tags/scripts/gemini_client/config.json
```

## 数据检查

查看当前 note：

```bash
ls -la note
```

查看 RL 过滤数据 summary：

```bash
cat dataset2/rl_0516_filter.summary.json
```

快速查看 jsonl 行数：

```bash
wc -l dataset2/rl_0516_filter.jsonl dataset2/sft_0516_4500.jsonl
```

抽查 jsonl 第一行：

```bash
head -1 dataset2/rl_0516_filter.jsonl
```

## RL 数据清洗

Gemini 4 次推理难度清洗：

```bash
/mmu_mllm_hdd/zhouhanshu/conda/envs/simple/bin/python \
  dataset2/run_rl_query_events_gemini_cleaning.py \
  --stage all \
  --model gemini-2.5-flash \
  --prompt-profile teacher_eval \
  --frames-per-step 5 \
  --text-scorer gemini \
  --judge-model gemini-2.5-flash \
  --repeats 4 \
  --workers 64 \
  --resume
```

重新按难度导出 `rl_0516_filter.jsonl`：

```bash
/mmu_mllm_hdd/zhouhanshu/conda/envs/simple/bin/python \
  dataset2/build_rl0516_filter.py
```

输出：

```text
dataset2/rl_0516_filter.jsonl
dataset2/rl_0516_filter.summary.json
```

## SFT 数据合成

0516 4500 teacher distill：

```bash
nohup bash dataset2/0516data/sft/run_dataset2_sft0516_4500_distill.sh \
  > logs/sft0516_4500_distill.out 2>&1 &
```

过滤并合并最终 SFT：

```bash
bash dataset2/0516data/sft/finalize_sft0516_4500.sh
```

输出：

```text
dataset2/sft_0516_4500.jsonl
```

## SFT 训练

进入 LLaMAFactory：

```bash
cd /mmu_mllm_hdd/zhouhanshu/test/exp3/streamweave_v5/SFT/LlamaFactory
```

启动 0516 4500：

```bash
bash configs/train_streamweave_0516_4500.sh
```

从最新 checkpoint 续跑：

```bash
RESUME=1 bash configs/train_streamweave_0516_4500.sh
```

当前主线输出目录：

```text
SFT/LlamaFactory/saves/qwen3-vl-8b/full/streamweave_sft_0516_4500
```

查看 checkpoint：

```bash
find saves/qwen3-vl-8b/full/streamweave_sft_0516_4500 \
  -maxdepth 1 -type d -name 'checkpoint-*' -printf '%f\n' | sort -V
```

查看最新训练日志：

```bash
tail -100 logs/sft0516_4500_20260516_195458/train.out
```

## RL 训练

进入仓库：

```bash
cd /mmu_mllm_hdd/zhouhanshu/test/exp3/streamweave_v5
```

当前 24GPU 主入口：

```bash
bash RL/scripts/train_exp9_24.sh
```

显式指定当前主线：

```bash
RUN_NAME=exp9_24 \
TRAIN_FILE=/mmu_mllm_hdd/zhouhanshu/test/exp3/streamweave_v5/dataset2/rl_0516_filter.jsonl \
VAL_FILE=/mmu_mllm_hdd/zhouhanshu/test/exp3/streamweave_v5/dataset2/rl_0515_val.jsonl \
SOURCE_MODEL_PATH=/mmu_mllm_hdd/zhouhanshu/test/exp3/streamweave_v5/models/qwen3vl_sft_0516_step100 \
bash RL/scripts/train_exp9_24.sh
```

多节点角色：

```bash
EXP9_24_RAY_ROLE=head   RAY_HEAD_IP=<head-ip> bash RL/scripts/train_exp9_24.sh
EXP9_24_RAY_ROLE=worker RAY_HEAD_IP=<head-ip> bash RL/scripts/train_exp9_24.sh
EXP9_24_RAY_ROLE=driver RAY_HEAD_IP=<head-ip> bash RL/scripts/train_exp9_24.sh
```

检查 Ray：

```bash
/mmu_mllm_hdd/zhouhanshu/conda/envs/verl_0425/bin/ray \
  status --address=<head-ip>:6379
```

当前 run 目录：

```text
RL/outputs/runs/exp9_24
```

看 RL 日志：

```bash
tail -100 RL/outputs/runs/exp9_24/train.log
```

看 checkpoint：

```bash
find RL/outputs/runs/exp9_24/checkpoints \
  -maxdepth 1 -type d -name 'global_step_*' -printf '%f\n' | sort -V
```

如果只想跑本地 judge smoke：

```bash
bash RL/scripts/train_exp9_localjudge.sh
```

如果只想跑训练链路 smoke：

```bash
bash RL/scripts/train_exp9_smoke.sh
```

## OVO 评测

OVO full 标注：

```text
/mmu_mllm_hdd/zhouhanshu/test/OVO-Bench/OVO-Bench/data/ovo_bench_new.json
```

OVO 1/8 标注：

```text
/mmu_mllm_hdd/zhouhanshu/test/OVO-Bench/OVO-Bench/data/ovo_bench_1of8_stratified.json
```

8 卡 vLLM 评测模板：

```bash
ANNO_PATH=/mmu_mllm_hdd/zhouhanshu/test/OVO-Bench/OVO-Bench/data/ovo_bench_new.json \
OUTPUT_DIR=outputs/ovo_<run_name> \
GPUS="0 1 2 3 4 5 6 7" \
WORKERS=16 \
bash scripts/run_ovo_8gpu_vllm.sh \
  /path/to/model
```

1/8 sanity 只替换：

```text
ANNO_PATH=/mmu_mllm_hdd/zhouhanshu/test/OVO-Bench/OVO-Bench/data/ovo_bench_1of8_stratified.json
OUTPUT_DIR=outputs/ovo_<run_name>_1of8
```

Gemini OVO full：

```bash
GOOGLE_APPLICATION_CREDENTIALS=/mmu_ssd3/group_lisize/hetu/xujia10/joint_tags/scripts/gemini_client/config.json \
/mmu_mllm_hdd/zhouhanshu/conda/envs/simple/bin/python \
  evaluation/eval_batch.py \
  --config configs/batch_ovo_gemini_full_0516.yaml \
  --benchmark ovo \
  --backend gemini \
  --model gemini-2.5-pro \
  --workers 128 \
  --output outputs/gemini_full_0516/results.jsonl \
  --worker-log-dir outputs/gemini_full_0516/worker_logs \
  --resume
```

看 OVO summary：

```bash
cat outputs/<run_name>/results_summary.txt
cat outputs/<run_name>/results_summary.json
```

## StreamingBench 评测

通用入口：

```bash
/mmu_mllm_hdd/zhouhanshu/conda/envs/simple/bin/python \
  evaluation/eval_batch.py \
  --config configs/eval_streamingbench.yaml
```

real split 建议使用 grouped eval，减少重复视频 rollout。具体配置以对应 `configs/*.yaml` 和 output 下的 `run_config.yaml` 为准。

看结果：

```bash
cat outputs/streamingbench_real_exp8_step40_grouped/results_summary.json
```

## Dataset3 反幻觉评测

VideoHallucer 两种 memory policy：

```bash
MODEL=/mmu_mllm_hdd/Models/Qwen3-VL-8B-Instruct
./dataset3/scripts/run_videohallucer_8gpu.sh "$MODEL" anchor_delta
./dataset3/scripts/run_videohallucer_8gpu.sh "$MODEL" delta_only
```

EventHallusion 两种 memory policy：

```bash
MODEL=/mmu_mllm_hdd/Models/Qwen3-VL-8B-Instruct
./dataset3/scripts/run_eventhallusion_8gpu.sh "$MODEL" anchor_delta
./dataset3/scripts/run_eventhallusion_8gpu.sh "$MODEL" delta_only
```

常用环境变量：

```text
GPUS="0 1 2 3 4 5 6 7"
WORKERS=16
LIMIT=8
TASK=semantic_detail
RESUME=1
OUTPUT_DIR=dataset3/outputs/<run_name>
```

看 summary：

```bash
cat dataset3/outputs/<run_name>/summary.txt
cat dataset3/outputs/<run_name>/summary.json
```

## 模型导出和加载检查

step200 SFT vLLM 导出模型加载检查：

```bash
/mmu_mllm_hdd/zhouhanshu/conda/envs/vllm/bin/python - <<'PY'
from transformers import AutoProcessor, AutoTokenizer

p = "models/qwen3vl8b_streamweave_sft_answered_full_anchor_delta_init_anchor_step200_vllm"
tok = AutoTokenizer.from_pretrained(p, trust_remote_code=True)
print("tokenizer_ok", type(tok).__name__, tok.eos_token, tok.pad_token)
proc = AutoProcessor.from_pretrained(p, trust_remote_code=True)
print("processor_ok", type(proc).__name__)
PY
```

如果 vLLM / Transformers 报：

```text
list has no attribute keys
```

优先检查导出模型的 `tokenizer_config.json` 里是否有 list 形式的 `extra_special_tokens`，必要时删除该字段，并从 base model 补齐 tokenizer / processor sidecar。

## GPU 和进程

查看 GPU：

```bash
nvidia-smi
```

查 RL / vLLM / Ray 相关进程：

```bash
pgrep -af 'verl.trainer.main_ppo|AgentLoopWorker|TaskRunner|WorkerDict|VLLM|vllm|ray::'
```

查 SFT 相关进程：

```bash
pgrep -af 'llamafactory-cli|torchrun|deepspeed'
```

查端口：

```bash
ss -ltnp | grep -E '8000|8001|8002|8003|8004|8005|8006|8007|6379|8265'
```

GPU 占位工具：

```bash
bash /mmu_mllm_hdd/zhouhanshu/test/gtest.sh status
bash /mmu_mllm_hdd/zhouhanshu/test/gtest.sh start --gpus 0-7
bash /mmu_mllm_hdd/zhouhanshu/test/gtest.sh stop
```

## Git 和记录

查看修改：

```bash
git status --short
```

查看单文件 diff：

```bash
git diff -- note/06-实验跑分.md
```

每次训练或评测完成后至少记录：

- 脚本和完整命令。
- 模型路径。
- 数据文件。
- 输出目录。
- checkpoint 或 summary 路径。
- 核心指标。
- errors / skipped / missing videos。
- 是否是 full、1/8、smoke 或 grouped eval。
